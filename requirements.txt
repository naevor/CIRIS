# CIRIS backend. Лёгкий набор: без scipy (FFT через numpy), без networkx (dict adjacency).
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
numpy>=1.26
pyyaml>=6.0
# опционально для sensorium:
psutil>=5.9
# sounddevice>=0.4   # раскомментируй для микрофона (RMS амплитуда, без записи)
